---
name: documentation
description: Guidelines for generating documentation and managing dependencies without DDEV wrappers.
---

# Documentation Guidelines

## Important

When generating documentation, do not use the 'ddev' command wrapper. Use vanilla command `php artisan` or `composer`.

When adding instructions for installing/uninstalling dependencies, use `composer require` and `composer remove` commands.
